const assert = require('assert');
const { Given, When, Then } = require('cucumber');

Given('I´m have {string} clients in my database', function (qtd) {
    return 'pending';
});


When('I access thed home page', function () {
    return 'pending';
});

Then('see the list of \{{string}} itens', function (qtd) {
    return 'pending';
});

let gql;_1e4‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers]]);_1e4‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const  typeDefs = gql`
    type Demand {
        id: ID!
        name: String!
        client: Client!
        deadline: String
    }

    extends type Query {
        demand: [Demand]!
    }
`;

       const resolvers = {
    Query: {
        demands: () => [],
    },
};
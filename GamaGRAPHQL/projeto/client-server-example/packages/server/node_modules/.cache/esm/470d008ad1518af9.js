let express,ApolloServer,gql,typeDefs;_d15‍.w("express",[["default",["express"],function(v){express=v}]]);_d15‍.w("apollo-server-express",[["ApolloServer",["ApolloServer"],function(v){ApolloServer=v}],["gql",["gql"],function(v){gql=v}]]);_d15‍.w("./graphql/typeDefs",[["default",["typeDefs"],function(v){typeDefs=v}]]);





const  app = express();

const server = new ApolloServer({
    typeDefs: typeDefs,
   resolvers: {
       Query: {
           demands: () => [],
       },
   },
});

server.applyMiddleware({
    app,
    cors: {
        origin: 'http:/localhost:3000'
    },
})

const PORT = process.env.PORT ? parseInt(process.env.PORT) : 8000;
const HOSTNAME = '127.0.0.1';

server.listen(PORT, HOSTNAME, () => {
    console.log(`Server is listening at http://${HOSTNAME}:${PORT}.`);
});
